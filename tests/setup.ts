// Test setup and utilities
import { beforeAll, afterAll } from 'bun:test'

// Global test setup
beforeAll(() => {
	// Setup test database, mocks, etc.
})

afterAll(() => {
	// Cleanup after tests
})
